# Frontend - Colegio Bernardo O'Higgins

Aplicacion web del sistema de gestion escolar, desarrollada con React 19 y Vite.

Para correr en desarrollo instalar dependencias con `npm install` y luego `npm run dev`. Requiere el gateway en el puerto 8080.

Para correr con Docker usar `docker compose up --build` desde la raiz del proyecto.
