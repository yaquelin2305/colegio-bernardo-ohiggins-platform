server:
  port: 8080 

spring:
  datasource:
    
    url: ${DATABASE_URL}
    username: ${DATABASE_USER}
    password: ${DATABASE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate 
    show-sql: false


security:
  jwt:
    enabled: true
    secret: ${JWT_SECRET}